# dotfiles

Personal dotfiles managed with GNU Stow. Works on both macOS and Linux (no root required).

## Setup

```bash
# Clone
git clone git@github.com:ygalron/dotfiles.git ~/dotfiles

# On Linux (no root needed):
./setup-linux.sh

# On macOS (manual stow):
cd ~/dotfiles && stow .
```
